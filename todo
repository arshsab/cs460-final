Server:
X   /key > gives server's verification key
X   /register > generates a signing + verification key and propogates
X   /vote > submit's vote and propogates
X   /results > returns the results of the election
    /ledger > returns the ledgeer of the votes
X   publish functions > pushes update to other nodes
X   /publish_registration > other servers send their registration here


Offline:
    Verify Election Ledger script
    Check vote script
    Create vote script
